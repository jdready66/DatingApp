/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { UsernameExistsValidatorDirective } from './usernameExistsValidator.directive';

describe('Directive: UsernameExistsValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new UsernameExistsValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
